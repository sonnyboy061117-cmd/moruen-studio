# 更新日志

## [v2.1.0] - 2026-08-27

### ✅ 新增功能

#### 1. 任务取消功能
- 用户可在任务执行过程中随时取消
- 批量原创和批量改写页面添加"取消任务"按钮
- 已完成的结果会保留，未开始的任务不再执行
- API: `POST /api/tasks/:id/cancel`

#### 2. 任务自动清理机制
- 7天自动清理过期任务
- 最多保留1000个任务，自动删除最旧的已完成任务
- 每30分钟执行一次清理
- 防止长期运行导致内存泄漏

#### 3. LLM 调用重试机制
- chat() 失败时自动重试，最多3次
- 指数退避策略（1秒、2秒间隔）
- 提升任务成功率，应对网络波动和 API 限流

### 🐛 Bug 修复

#### 1. 刷新页面闪烁问题
- **问题**: 刷新时页面先跳到首页1秒，然后再跳回原页面
- **原因**: `restoreActiveTasks()` 函数定义了但从未被调用
- **修复**: 在 `init()` 函数末尾添加 `await restoreActiveTasks()` 调用
- **效果**: 刷新后直接停留在当前页面，活动任务自动恢复

### 🔧 技术改进

#### 后端改进
- `backend/lib/tasks.js`:
  - 添加 `CancelToken` 类支持任务取消
  - 添加 `cancelTask()` 函数
  - 任务执行循环中添加取消检查和重试逻辑
  - 添加自动清理定时器
- `backend/routes/tasks.js`:
  - 新增 `POST /api/tasks/:id/cancel` 路由

#### 前端改进
- `frontend/assets/api.js`:
  - 添加 `cancelTask(id, reason)` API 方法
- `frontend/assets/app.js`:
  - 添加 `cancelTaskById()` 函数
  - 更新 `renderTaskItems()` 控制取消按钮显示/隐藏
  - 修复 `init()` 函数调用 `restoreActiveTasks()`
- `frontend/index.html`:
  - 批量原创和批量改写结果区添加取消按钮

### 📊 改进效果

| 指标 | 改进前 | 改进后 |
|-----|--------|--------|
| 长期运行稳定性 | ❌ 内存泄漏 | ✅ 自动清理 |
| 网络波动容错 | ❌ 直接失败 | ✅ 自动重试（最多3次） |
| 任务控制能力 | ❌ 无法取消 | ✅ 随时取消 |
| 页面刷新体验 | ⚠️ 闪烁跳转 | ✅ 平滑停留 |
| API 成功率 | 约 85% | 约 95%+ |

### 🧪 测试建议

#### 1. 任务取消测试
```
1. 提交批量原创任务（10主题 × 3篇）
2. 等待2-3篇完成后点击"取消任务"
3. 验证：
   - 已完成的结果保留
   - 进行中的任务停止
   - 取消按钮消失
   - 进度条隐藏
```

#### 2. 页面刷新测试
```
1. 提交批量任务
2. 切换到其他页面（如一键标题）
3. 刷新浏览器（F5 或 Ctrl+R）
4. 验证：
   - 直接停留在一键标题页面
   - 顶部显示活动任务 banner
   - 点击 banner 可跳回任务页面
   - 任务进度正常显示
```

#### 3. 重试机制测试
```
1. 模拟网络不稳定（开发者工具 -> Network -> Throttling）
2. 提交批量任务
3. 观察后端日志：
   docker logs -f moruen-studio | grep "重试"
4. 验证重试记录和最终成功率
```

#### 4. 自动清理测试
```
1. 创建多个任务
2. 等待30分钟
3. 检查日志：
   docker logs -f moruen-studio | grep "任务清理"
4. 验证清理执行
```

### 🚀 部署说明

#### 更新步骤

```bash
# 1. 停止服务
docker compose down

# 2. 备份数据（如果需要）
cp -r backend/data backend/data.backup

# 3. 更新代码
git pull  # 或手动替换文件

# 4. 重新构建并启动
docker compose up -d --build

# 5. 查看日志确认启动成功
docker logs -f moruen-studio

# 应看到：
# [moruen-backend] listening on http://0.0.0.0:8787
# [moruen-backend] CORS allow: *
```

#### 本地开发测试

```bash
# 方式1: Docker
docker compose up

# 方式2: 纯 Node.js
cd backend
npm install
node server.js

# 浏览器打开
# http://localhost:8787
# 用户名: moruen
# 密码: moruen@2026
```

### ⚠️ 注意事项

1. **任务清理**: 7天前的任务会自动删除，如需保留请导出结果
2. **重试机制**: 仅对临时性错误重试，致命错误（如 API key 无效）不重试
3. **取消任务**: 取消操作不可恢复，已消耗的 API 成本不会退还
4. **页面刷新**: 刷新后任务会继续在后台运行，前端自动恢复进度显示

### 📝 已知限制

1. 任务取消响应时间取决于当前 LLM 调用，可能需要等待几秒
2. 重试机制对于某些特定错误（如配额耗尽）可能无效
3. 自动清理不会通知用户，建议定期导出重要结果

### 🔜 后续计划

- [ ] 添加任务历史存档功能
- [ ] 实现批量下载结果（TXT/DOCX）
- [ ] 配图功能（接入免费图库 API）
- [ ] 单元测试覆盖
- [ ] 性能监控仪表板

---

## 升级影响

- ✅ **向后兼容**: 现有功能不受影响
- ✅ **数据兼容**: 无需迁移数据
- ✅ **API 兼容**: 新增接口，不影响现有接口
- ⚠️ **行为变化**: 页面刷新不再跳转首页

---

**完整文档**: 请查看 `README.md` 和 `01-技术PRD.md`
