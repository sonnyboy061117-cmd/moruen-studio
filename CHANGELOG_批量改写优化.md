# 批量改写功能优化 - 2026-09-02

## 优化目标
解决批量改写后文章**结构雷同**和**风格选择区分度不够**的问题

## 主要改进

### 1. 新增文章结构模板系统
**文件**: `backend/lib/prompts.js`

新增三种差异化的文章结构模板：

#### 结论先行型
- 开门见山给出核心观点（前10%）
- 分点展开论据/案例（中间75%）
- 结尾升华或行动建议（后15%）
- 适用场景：干货型文章、方法论

#### 故事引入型
- 具体场景/故事开头（前20%）
- 自然转折到观点（10%）
- 展开方法/观点（55%）
- 行动号召结尾（15%）
- 适用场景：经验分享、故事叙述

#### 问题拆解型
- 提出问题/困境（前15%）
- 分析问题原因（35%）
- 给出解决方案（40%）
- 总结要点（10%）
- 适用场景：问题解决、深度分析

**自动差异化机制**：
- 每次改写随机选择一个结构模板
- 强制要求与原文开头方式不同
- 段落顺序必须重新排列

### 2. 增强风格选择真实区分度
**文件**: `backend/lib/prompts.js`

新增四种明确区分的写作风格：

| 风格 | 语气基调 | 句式特征 | 用词风格 |
|------|---------|---------|---------|
| **专业分析型** | 客观、理性、数据导向 | 陈述句为主，多用"数据显示""研究表明" | 正式但不生硬，可用行业术语 |
| **经验分享型** | 第一人称，像老师傅传授经验 | 多用"我发现""我试过""后来才明白" | 口语化，"说实话""坦白讲" |
| **故事叙述型** | 画面感强，讲完整故事 | 场景描写（时间/地点/对话），长短句营造节奏 | 多用动词少用形容词 |
| **对话问答型** | 像在回答读者问题 | 每段用"你可能会问""那怎么办"引出 | 用"你""咱们"拉近距离 |

每种风格都有明确的约束条件，确保全文风格一致性。

### 3. 相似度自检系统（可选质检）
**文件**: `backend/lib/similarity.js`

实现三维度相似度检测：

#### 结构相似度（权重40%）
- 段落数量对比
- 开头方式检测（提问/故事/结论）
- 结尾方式检测（行动建议/总结）
- 首段长度差异
- 平均段落长度差异

#### 句式相似度（权重30%）
- 句子数量对比
- 句子长度分布
- 标点符号模式匹配

#### 词汇重复度（权重30%）
- 关键词重叠率（排除停用词）
- 连续词组（bigram）重复检测

**阈值设定**：
- 综合相似度 < 55%：通过 ✓
- 综合相似度 ≥ 55%：警告 ⚠️ 建议重新生成

**检测结果展示**：
```
【相似度检测】
综合相似度: 48% ✓ 通过
- 结构相似度: 35%
- 句式相似度: 52%
- 词汇重复度: 58%
```

### 4. 前端展示优化
**文件**: `frontend/assets/app.js`

批量改写结果新增相似度徽章显示：
- ✓ 相似度 48%（绿色）：通过
- ⚠️ 相似度 62%（橙色）：过高，建议重新生成
- 鼠标悬停显示详细警告信息

## 集成流程

### 改写流程增强
```
用户提交批量改写
    ↓
随机/按风格选择结构模板
    ↓
LLM改写（应用结构约束 + 风格约束）
    ↓
相似度检测（三维度评分）
    ↓
降AI味处理（如果开启）
    ↓
返回结果 + 相似度报告
```

### API响应格式变化
批量改写任务的每个item新增字段：
```json
{
  "id": "...",
  "title": "...",
  "body": "...",
  "score": 27,
  "similarity": {
    "score": 48,
    "structure": 35,
    "sentence": 52,
    "vocab": 58,
    "passed": true,
    "warnings": []
  },
  "similarityWarning": null
}
```

## 使用方法

### 1. 手动指定结构（API参数）
```javascript
await api.batchRewrite({
  sources: ['文章1', '文章2'],
  structure: '故事引入',  // 可选：结论先行、故事引入、问题拆解
  strength: '深度',
  // ...其他参数
});
```

### 2. 自动随机结构（默认）
```javascript
await api.batchRewrite({
  sources: ['文章1', '文章2'],
  // 不传structure参数，系统自动随机选择
});
```

### 3. 风格选择（前端已有）
前端选择框的风格（专业分析、经验分享、故事叙述、对话问答）会自动映射到对应的写作约束。

## 预期效果

### 改进前
- ❌ 5篇改写文章结构高度相似
- ❌ 都是"首先...其次...最后..."模式
- ❌ 风格选择没有实质区别
- ❌ 无法评估改写质量

### 改进后
- ✅ 每篇文章使用不同结构模板
- ✅ 开头方式多样化（场景/提问/结论）
- ✅ 风格有明确的句式和用词区别
- ✅ 实时相似度检测，质量可量化

## 性能影响
- 相似度检测耗时：~5-10ms/篇
- 对总体改写速度影响：< 1%
- 内存开销：可忽略不计

## 后续优化方向（第二期）

1. **智能结构推荐**
   根据原文特征自动推荐最适合的结构模板

2. **相似度阈值可配置**
   允许用户在后台设置相似度警告阈值

3. **自动重试机制**
   相似度过高时自动用另一个结构模板重新生成

4. **更多结构模板**
   增加"对比分析型""时间线型""清单型"等模板

5. **A/B测试**
   生成2个版本让用户选择保留哪个

## 技术细节

### 停用词过滤
相似度检测会排除常见停用词（的、了、在、是...），只比较关键词。

### Bigram检测
检测连续两个词的组合，例如"人工智能"+"发展趋势"→"人工智能发展趋势"。

### 实体名保护
所有人名、地名、机构名、品牌名在改写时保持原样，不影响相似度评分。

## 测试建议

1. 准备3篇结构不同的文章（一篇故事开头、一篇问题开头、一篇结论开头）
2. 每篇改写3次，观察结构多样性
3. 检查相似度报告是否准确
4. 验证风格选择是否有实质区别

## 回滚方案
如果新版本有问题，可以回滚到之前版本：
```bash
git checkout HEAD~1 backend/lib/prompts.js
git checkout HEAD~1 backend/lib/similarity.js
git checkout HEAD~1 backend/lib/tasks.js
git checkout HEAD~1 frontend/assets/app.js
```
