# 万能改写重构 - 试点上线方案

## 📋 四个关键问题的解决方案

### 1. ✅ 调用次数不会翻倍
**澄清**：两阶段改写是在**一个Prompt内完成**，不是真的调用2次API。

```javascript
// 这是一个完整的Prompt，只调用1次API
const prompt = `
【第一阶段：提炼】
请先从原文中提炼核心论点、事实、基调...

【第二阶段：重组】
基于提炼结果重新组织文章...

原文: ${text}
`;

// 只调用1次
const result = await chat({ messages: [{ role: 'user', content: prompt }] });
```

**成本影响**：
- ❌ 不会翻倍
- ✅ 仅增加10-15%（因为Prompt从~200 tokens增加到~400 tokens）
- 目前中转站费用对方承担，暂无影响

---

### 2. ✅ 响应时间会变长 - 已优化提示

**预期影响**：响应时间增加20-30%

**前端优化**：
- ✅ 更新加载提示：`"正在深度重组改写（预计30-60秒）..."`
- ✅ 结果标题改为：`"深度重组改写"` 而非 `"智能改写"`
- ✅ 管理用户预期：明确告知需要更长处理时间

---

### 3. ✅ 避免过度承诺 - 已调整文案

**新增风险提示框**：
```
💡 重要说明
• 结构重组：改写会调整段落顺序和开头方式，大幅降低结构相似度
• 风险提示：微信原创检测算法不公开，无法100%保证不被判定，但本功能可显著降低判定风险
• 建议：发布前人工检查段落顺序、论据位置是否与原文有明显差异
```

**文案对比**：

| 改进前 | 改进后 |
|--------|--------|
| ❌ "完全不会被判定非原创" | ✅ "大幅降低结构相似度" |
| ❌ "绝对安全" | ✅ "显著降低判定风险" |
| ❌ "100%通过" | ✅ "建议发布前人工检查" |

---

### 4. ✅ 分阶段试点上线 - 已实现开关

**试点策略**：只在"深度"和"彻底重写"强度上启用两阶段重组

#### 配置文件：`backend/lib/prompts.js`

```javascript
// 两阶段重组功能开关配置
export const REWRITE_CONFIG = {
  // 试点阶段：只在"深度"和"彻底重写"启用
  enabledStrengths: ['深度', '彻底重写'],

  // 全量上线后改为：['轻度', '中度', '深度', '彻底重写']
  // 或设为 null 表示全部启用
};
```

#### 行为对比

| 改写强度 | 旧版逻辑（保留） | 新版逻辑（试点） | 状态 |
|---------|---------------|----------------|------|
| **轻度** | ✅ 简单同义替换 | ❌ 不启用两阶段 | 保持不变 |
| **中度** | ✅ 简单同义替换 | ❌ 不启用两阶段 | 保持不变 |
| **深度** | ❌ 旧版被替换 | ✅ 启用两阶段重组 | **试点中** |
| **彻底重写** | ❌ 旧版被替换 | ✅ 启用两阶段重组 | **试点中** |

#### 全量上线时的操作

验证通过后，修改配置：
```javascript
export const REWRITE_CONFIG = {
  enabledStrengths: null,  // null = 全部启用
};
```

或者：
```javascript
export const REWRITE_CONFIG = {
  enabledStrengths: ['轻度', '中度', '深度', '彻底重写'],  // 明确列出
};
```

---

## 📊 试点验证计划

### 第一阶段：内部测试（1-2天）
1. 用3-5篇实际文章测试"深度"改写
2. 人工对比段落顺序、开头方式是否改变
3. 检查关键事实（人名、时间、数据）是否保留
4. 测试响应时间是否在可接受范围内

**通过标准**：
- ✅ 段落顺序有明显调整
- ✅ 开头方式与原文不同
- ✅ 关键事实100%保留
- ✅ 响应时间 < 90秒

### 第二阶段：小范围用户测试（3-5天）
1. 选择5-10个活跃用户内测
2. 收集反馈：改写效果、响应时间、满意度
3. 监控：是否有用户反馈"被判定非原创"

**通过标准**：
- ✅ 用户满意度 > 80%
- ✅ 无"被判非原创"投诉
- ✅ 响应时间用户可接受

### 第三阶段：全量上线
1. 修改配置启用所有强度
2. 前端显著提示"新版改写功能"
3. 持续监控1周，收集反馈

---

## 🔧 快速回滚方案

如果试点期间发现问题，立即回滚：

### 方案1：关闭试点功能
```javascript
// backend/lib/prompts.js
export const REWRITE_CONFIG = {
  enabledStrengths: [],  // 空数组 = 全部禁用，恢复旧版
};
```

### 方案2：Git回滚
```bash
cd /d/moruen-studio-v2
git checkout HEAD~1 backend/lib/prompts.js
git checkout HEAD~1 backend/routes/generate.js
git checkout HEAD~1 frontend/index.html
git checkout HEAD~1 frontend/assets/app.js
npm run dev
```

---

## 📈 监控指标

试点期间需要监控：

| 指标 | 旧版基线 | 新版目标 | 监控方式 |
|------|---------|---------|---------|
| **平均响应时间** | 20-30秒 | < 45秒 | 后端日志 |
| **用户投诉率** | - | < 2% | 客服反馈 |
| **非原创判定率** | 高（用户反馈） | 显著下降 | 用户回访 |
| **改写满意度** | - | > 80% | 用户问卷 |

---

## 🎯 验收标准

### 功能层面
- [x] "深度"和"彻底重写"启用两阶段重组
- [x] "轻度"和"中度"保持旧版逻辑
- [x] 开关配置生效，可快速调整
- [x] 前端风险提示清晰展示

### 用户体验层面
- [x] 加载提示明确告知预计时间
- [x] 文案不过度承诺
- [x] 结构选择器可用
- [x] 风格选择器可用

### 技术层面
- [x] 只调用1次API（不翻倍）
- [x] 关键事实保护逻辑生效
- [x] 错误处理完善
- [x] 回滚方案可用

---

## 📝 用户沟通话术

### 内测邀请
```
您好！我们正在测试"深度改写"的新版本，
可以大幅调整文章结构和段落顺序，降低与原文的相似度。
邀请您参与内测，体验新功能！

注意事项：
• 新版改写需要30-60秒，比之前稍慢
• 段落顺序会有明显调整
• 无法100%保证微信不判定，但可显著降低风险
• 发布前建议人工检查一下效果

如有任何问题，请随时反馈！
```

### 功能上线公告
```
【功能升级】万能改写 - 深度重组模式

✨ 新增功能：
• 支持调整段落顺序和开头方式
• 可选择文章结构（结论先行/故事引入/问题拆解）
• 可选择写作风格（专业分析/经验分享/故事叙述/对话问答）

⏱️ 温馨提示：
• 深度改写需要30-60秒，请耐心等待
• 建议发布前人工检查改写效果
• 轻度/中度改写保持原有速度

🔒 风险说明：
• 微信检测算法不公开，本功能可显著降低判定风险，但无法100%保证
• 建议搭配人工检查使用
```

---

## 🚀 上线 Checklist

### 试点前
- [x] 代码已提交并测试
- [x] 配置开关设置为"深度"和"彻底重写"
- [x] 前端风险提示已添加
- [x] 加载提示已优化
- [x] 回滚方案已验证

### 试点中
- [ ] 每天检查错误日志
- [ ] 收集用户反馈
- [ ] 记录响应时间数据
- [ ] 跟踪"非原创判定"反馈

### 全量上线前
- [ ] 试点数据达标
- [ ] 无重大bug
- [ ] 用户反馈正面
- [ ] 修改配置启用全部强度

---

## 📞 问题应急预案

### 场景1：响应时间过长（>90秒）
**解决方案**：
1. 检查中转站API是否正常
2. 临时关闭试点功能
3. 优化Prompt长度

### 场景2：用户投诉"仍被判非原创"
**应对话术**：
```
感谢您的反馈！微信的检测算法不公开，
我们的功能只能显著降低风险，无法100%保证。

建议：
1. 使用"彻底重写"强度
2. 发布前人工调整1-2个段落顺序
3. 适当增删部分内容

如需退款，我们全额处理。
```

### 场景3：关键事实丢失
**紧急处理**：
1. 立即回滚到旧版
2. 修复Prompt中的"实体保护"逻辑
3. 重新测试后再上线

---

## 总结

这个试点方案的核心优势：

1. ✅ **渐进式**：只在"深度"和"彻底重写"试点，不影响轻度/中度用户
2. ✅ **可控制**：通过配置开关可快速启用/关闭
3. ✅ **可回滚**：出问题可立即恢复旧版
4. ✅ **成本可控**：不是2次调用，成本仅增加10-15%
5. ✅ **用户预期管理**：明确告知响应时间和风险

建议按此方案执行，每个阶段通过验收标准后再进入下一阶段。
